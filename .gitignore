# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# build
dist/
*.tsbuildinfo

# env
.env
.env*.local

# editors
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo

# os
.DS_Store
Thumbs.db

# turbo
.turbo/

# script audit logs (e.g. one-off comms send tracking)
apps/web/scripts/.*-sent.json

# vercel
.vercel/

# debug
npm-debug.log*
pnpm-debug.log*
.vercel
.env.vercel-check

# founder-local agent trackers (kept local, mirror t2000's convention — not committed)
HANDOFF_NEXT_AGENT.md
NEXT_AGENT_KICKOFF.md

# local-only redesign artifacts (kept out of the repo on purpose)
# Covers any design handoff bundle (handoff_audric, handoff_username_flow, future bundles).
design_handoff_*/
scripts/
# but track tooling under apps/web-v2/scripts (g6 smoke verifier, etc.)
!apps/web-v2/scripts/
!apps/web-v2/scripts/**

# pitch-deck iterations (marketing artifacts, not part of the app)
pitch-deck.html
pitch-deck-v*.html
PORTFOLIO_REGRESSION_MATRIX.md
