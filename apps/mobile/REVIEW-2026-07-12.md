# Founder review — 2026-07-12 (funkii + agent session)

> Read this BEFORE resuming N1/B2. It corrects two stale facts in `HANDOFF.md`,
> hands you a fully-verified local auth bench, and pins the one code bug that
> blocks the Phase-0 parity test. Written against `feat/mobile-app` merged with
> `main` (merge commit in this branch; one trivial `pnpm-lock.yaml` conflict).

---

## 1. The blocker is now a CODE bug, not config (N1)

**FIXED (2026-07-12 evening, funkii session):** `src/auth/google.ts` now mirrors
`packages/auth/src/client.ts` `startLogin` — ephemeral Ed25519 keypair → Enoki
`createZkLoginNonce` → `nonce` on the Google authorize URL. Pending material
(ephemeral secret, randomness, maxEpoch) persists in SecureStore via
`src/auth/pending-auth.ts` for B2 signing. Requires
`EXPO_PUBLIC_ENOKI_API_KEY` in mobile `.env.local` (same as web-v3's public
Enoki key). **Rebuild Metro after pulling** (`expo start --clear`).

**Verify step (still required to close Phase 0):** sign in on the simulator →
exchange returns `{ address, token }` (no `salt_failure`) → address matches
audric.ai for the same Google account.

## 2. Credential corrections (HANDOFF.md §"credentials" is stale)

- **Canonical Google client = `396016292233-h1ch9iqtkpnhatm589ca0mnvjo5b3dpp`**
  ("audric"). Verified by extracting the client id from the LIVE audric.ai JS
  bundle. The `evvduh…` id in your handoff ("audric-dev", created Jul 2) is NOT
  what prod runs — testing against it would fork the wallet. Same for the Enoki
  key: prod bundle ships `enoki_public_38d5a4c5…` (matches funkii's local).
- **RESOLVED (funkii, 2026-07-12 evening): the `h1ch9…` secret was RECOVERED**
  via Google's client-secret rotation — "Add Secret" on the client details
  page (right column) minted a fresh secret; the lost March-23 one still exists
  alongside it (disable → delete after a quiet week). The new secret is
  verified VALID against Google's token endpoint and lives in funkii's + this
  worktree's `.env.local`. **The aud-migration plan from earlier today is
  CANCELLED — `h1ch9…` is canonical forever, no wallet re-derivation, ever.**
- **Develop against `h1ch9…` (the bench is already flipped):** this worktree's
  web-v3 + mobile `.env.local` point at the prod client with the working
  secret. `http://localhost:3002/auth/bridge` is registered on it, and Enoki
  already trusts it. Your Phase-0 parity test is now the REAL one — mobile
  address vs audric.ai address, same Google account, must match exactly.
- The extra clients created during the hunt (`audric-dev`/`evvduh…`,
  `audric-v3`/`nf9vp…`) are now disposable — see the cleanup ledger (§7).
- Ship note: when the exchange deploys to prod, set `GOOGLE_CLIENT_SECRET`
  (the new one) in Vercel — prod has never carried it (nothing else uses it).

## 3. Branch drift (resolved here) + catalog drift (yours to fix)

- `main` moved 206 commits past your merge-base (store purge, Stripe billing,
  proxy split, model bumps). Merged into this branch: only `pnpm-lock.yaml`
  conflicted (took main's + `pnpm install`). Mobile typecheck stays clean.
- **`src/app-state/catalog.ts` hardcodes copies of web-v3 catalogs and they
  have already drifted:**
  - switcher has `anthropic/claude-sonnet-4.6`; web-v3 main ships
    `anthropic/claude-sonnet-5`
  - Plans sheet is display-only; web-v3 main sells Pro/Max via Stripe
    (`lib/credit/tiers.ts` now carries `priceEnv` price IDs)
  - the `lib/skills/catalog.ts` you mirrored no longer exists on main
  Recommendation (matches CLAUDE.md single-source rule): web-v3 serves
  models/tiers/skills as small JSON endpoints; mobile fetches with the
  hardcodes as offline fallback.

## 4. UI/UX gaps — FIXED IN THIS BRANCH (funkii review session)

Shipped here (typechecked + verified on the simulator); diff is yours to keep
in sync:

1. **Fake carousel sign-in removed** — the Welcome step's mock "Continue with
   Google" (1.7s fake spinner → fake persona) is now a "Get started" advance;
   "Skip for now" finishes onboarding WITHOUT demoting a signed-in user to
   guest. `gate.tsx` is the single sign-in surface. The orphaned store
   machinery (`onGoogle`, `signingIn`, `googleTimer`, `skipToApp`,
   `signInFromNudge`) was removed.
2. **Nudge dialog now routes to REAL sign-in** — its "Continue with Google"
   clears the session (`useAuth().signOut`) so the router lands on the gate,
   instead of flipping a fake guest bit. (Note: with `skipToApp` gone, `guest`
   can currently never become true — the nudge is unreachable until you build
   a real anonymous path, if web parity ever needs one on mobile.)
3. **Fake context meter hidden** — `composer.tsx` `ctxShow` is now `false`
   with a wiring TODO (surface real usage from the stream finish part, then
   restore the condition). The static `CTX` catalog data stays for that wiring.
4. **Theme seam fixed** — `gate.tsx` converted from the legacy hard-dark
   palette to `useTheme()` (light/dark like the rest of the app); the legacy
   `colors` + `font` exports in `theme/tokens.ts` were removed (gate was the
   last consumer).
5. **Model catalog bumped** — `anthropic/claude-sonnet-4.6` →
   `anthropic/claude-sonnet-5` ("Claude Sonnet 5") in `catalog.ts` MODELS +
   VISION_MODELS, matching web-v3 main.

Still known-mock (your own PROD_READINESS list, unchanged): wallet tab,
memory toggle backend, demo tool cards, local-only votes.

Also noted: iOS keychain persists SecureStore across app reinstalls (session
survives delete/reinstall). Fine for users, surprising in tests — `xcrun
simctl keychain <udid> reset` is the reset lever.

## 5. Verified working in this session (no action)

Chat streaming end-to-end (reasoning, suggestions, follow-ups, persistence),
drawer/history, wallet/settings/skills screens render faithfully, guest mode,
exchange route input validation + rate limiting, B1 session mint/verify code
review (solid), model switcher UX. Dev servers: web-v3 worktree on :3002,
Metro on :8081.

## 6. Suggested order

1. Nonce fix (§1) → **real Phase-0 parity**: mobile-derived address vs
   audric.ai (same Google account) — the bench runs the prod client now.
2. B1 runtime verify (mint → Bearer → data routes) on the same bench.
3. B2 exactly as you sequenced (balance read-only → receive → send), behind
   the Phase-0 pass from step 1.
4. Catalog drift (§3).

## 7. Auth cleanup ledger (who owns what)

- funkii, after a quiet week: on the `audric` client, DISABLE then delete the
  lost March-23 secret (`****3Mqy`) — nothing can be using it (it was lost;
  Vercel never carried it). Keep the new `****cAaK` secret only.
- funkii, later: delete the now-disposable `audric-dev` (`evvduh…`) and
  `audric-v3` (`nf9vp…`) clients + the `tail46791a.ts.net` authorized domain
  once Rowan confirms his local env is on `h1ch9…`; remove the `nf9vp…` Enoki
  provider at the same time. Purge the dead suffixed secrets from `.env.local`.
- funkii, optional 10s: register `http://localhost:3002/api/mobile-auth/bridge`
  on the `audric` client — then delete your bridge-alias scaffolding (the
  `85d0c6e` chore commit: `app/auth/bridge/route.ts`, `MOBILE_AUTH_BRIDGE_PATH`,
  `EXPO_PUBLIC_BRIDGE_URL`) and run canonical paths everywhere.
- Already done this session: temp debug logs reverted; stray env lines removed;
  worktree envs on the prod client + recovered secret; review artifacts here.
